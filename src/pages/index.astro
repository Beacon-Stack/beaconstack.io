---
import Base from '../layouts/Base.astro';
---

<Base
  title="The Beacon Stack — self-hosted media stack"
  active="home"
>

  <!-- ═══════════════════════════════════════════════
       HERO — centered, luminarr-family structure
  ═══════════════════════════════════════════════ -->
  <section class="hero">
    <!-- Radial glow -->
    <div class="hero-glow" aria-hidden="true"></div>

    <div class="container">
      <div class="hero-center">

        <!-- Badge pill -->
        <div class="hero-badge">
          <span class="live-dot" aria-hidden="true"></span>
          open source &middot; MIT &middot; zero telemetry
        </div>

        <!-- Gradient h1 -->
        <h1 class="hero-title">
          <span class="gradient-text">The Beacon Stack</span>
        </h1>

        <!-- Subtitle -->
        <p class="hero-sub">
          TV, movies, and BitTorrent — each app works standalone, and all
          three share indexers, quality profiles, download clients, and
          media-handling settings through Pulse when you want them to. One&nbsp;<code>docker&nbsp;compose&nbsp;up</code>
          for the whole stack.
        </p>

        <!-- CTA buttons -->
        <div class="hero-actions">
          <a
            href="https://github.com/beacon-stack"
            class="btn btn--primary"
            target="_blank"
            rel="noopener noreferrer"
          >
            View on GitHub &rarr;
          </a>
          <a href="#what-is-the-stack" class="btn btn--outline">
            Read the docs
          </a>
        </div>

        <!-- Terminal block -->
        <div class="hero-terminal terminal">
          <div class="terminal-bar">
            <span class="terminal-dot terminal-dot--red"></span>
            <span class="terminal-dot terminal-dot--yellow"></span>
            <span class="terminal-dot terminal-dot--green"></span>
            <span class="terminal-title">beacon-stack/deploy</span>
            <button class="terminal-copy-btn" type="button" aria-label="Copy command">Copy</button>
          </div>
          <div class="terminal-body">
            <span class="syn-prompt">$ </span><span class="syn-cmd">docker compose up</span> <span class="syn-flag">-d</span>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════
       STATS STRIP
  ═══════════════════════════════════════════════ -->
  <section class="stats-section reveal">
    <div class="container">
      <div class="stats-strip">
        <div class="stat-item">
          <span class="stat-value">4</span>
          <span class="stat-label">apps in the stack</span>
        </div>
        <div class="stat-divider" aria-hidden="true"></div>
        <div class="stat-item">
          <span class="stat-value">1</span>
          <span class="stat-label">control plane</span>
        </div>
        <div class="stat-divider" aria-hidden="true"></div>
        <div class="stat-item">
          <span class="stat-value">0</span>
          <span class="stat-label">telemetry</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════
       WHAT'S IN THE STACK — 4-card grid
  ═══════════════════════════════════════════════ -->
  <section class="stack-section" id="what-is-the-stack">
    <div class="container">
      <div class="section-header reveal">
        <p class="section-label">The stack</p>
        <h2 class="section-heading">Four apps, one config layer</h2>
        <p class="section-lead">
          Pilot, Prism, and Haul each run independently with local configuration.
          Pulse is the hub that lets all three share one indexer list, one set of
          quality profiles, one download client (Haul registers itself), and a
          common media-handling settings namespace.
        </p>
      </div>

      <div class="app-grid">

        <!-- Pulse — visually distinguished as hub -->
        <a href="/pulse" class="app-card app-card--pulse reveal">
          <div class="app-card-header">
            <div class="icon-chip">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
              </svg>
            </div>
            <div>
              <h3 class="app-name">Pulse</h3>
              <span class="app-tag">control plane</span>
            </div>
          </div>
          <p class="app-desc">
            Service registry and shared configuration layer. Beacon apps register here,
            but so can anything else — the Go SDK at <code>pkg/sdk</code> lets any
            service tap into shared indexers, quality profiles, download clients, and
            media-handling settings. Optional for a single install, essential the
            moment you run two.
          </p>
          <span class="app-port">:9696</span>
          <span class="app-link">Deep dive &rarr;</span>
        </a>

        <!-- Pilot -->
        <a href="/pilot" class="app-card reveal">
          <div class="app-card-header">
            <div class="icon-chip">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <rect x="2" y="7" width="20" height="15" rx="2" ry="2"/>
                <polyline points="17 2 12 7 7 2"/>
              </svg>
            </div>
            <div>
              <h3 class="app-name">Pilot</h3>
              <span class="app-role">TV series manager</span>
            </div>
          </div>
          <p class="app-desc">
            Tracks a TV library via TMDB, polls indexers for new episodes, grabs through
            your download client, and files completed downloads into season folders.
            Dead-torrent detection with auto-blocklist, and one-pass Sonarr import.
          </p>
          <span class="app-port">:8383</span>
          <span class="app-link">Deep dive &rarr;</span>
        </a>

        <!-- Prism -->
        <a href="/prism" class="app-card reveal">
          <div class="app-card-header">
            <div class="icon-chip">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <polygon points="12 2 2 7 12 12 22 7 12 2"/>
                <polyline points="2 17 12 22 22 17"/>
                <polyline points="2 12 12 17 22 12"/>
              </svg>
            </div>
            <div>
              <h3 class="app-name">Prism</h3>
              <span class="app-role">Movie collection manager</span>
            </div>
          </div>
          <p class="app-desc">
            Edition-aware release scoring, full release decision explainability, and a
            native Radarr v3 API so Overseerr and Jellyseerr connect without modification.
            Import from a running Radarr instance in one pass.
          </p>
          <span class="app-port">:8282</span>
          <span class="app-link">Deep dive &rarr;</span>
        </a>

        <!-- Haul -->
        <a href="/haul" class="app-card reveal">
          <div class="app-card-header">
            <div class="icon-chip">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                <polyline points="7 10 12 15 17 10"/>
                <line x1="12" y1="15" x2="12" y2="3"/>
              </svg>
            </div>
            <div>
              <h3 class="app-name">Haul</h3>
              <span class="app-role">BitTorrent client</span>
            </div>
          </div>
          <p class="app-desc">
            Built on anacrolix/torrent. Accurate ETAs from exponential moving averages,
            multi-level stall detection that catches dead torrents the rest of the
            stack misses, and rename-on-complete using media metadata passed by
            Pilot or Prism. VPN-aware dashboard.
          </p>
          <span class="app-port">:8484</span>
          <span class="app-link">Deep dive &rarr;</span>
        </a>

      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════
       DATA FLOW ANIMATION — Pulse hub
  ═══════════════════════════════════════════════ -->
  <section class="flow-section">
    <div class="container">
      <div class="flow-layout">

        <div class="flow-text reveal">
          <p class="section-label">How it connects</p>
          <h2 class="section-heading">How Pulse connects everything</h2>
          <p class="section-lead">
            Each Beacon service registers with Pulse on startup and heartbeats every
            30 seconds. Shared state — indexers, quality profiles, download clients,
            media-handling settings — flows outward from Pulse to every subscribed
            service on a 30-second sync, and immediately when Pulse fires a push
            hook on save. When Haul registers, Pulse auto-creates the download-client
            entry so Pilot and Prism find it without anyone copying an API key.
          </p>
          <p class="section-lead" style="margin-top:0.75rem;">
            Pulse isn't Beacon-only. Any Go service can join through the client SDK
            at <code>pkg/sdk</code> — register, discover peers, pull assigned
            indexers, and read shared config with one client instance.
          </p>
        </div>

        <div class="flow-anim-wrap reveal">
          <div class="anim-panel" role="img" aria-label="Diagram: Pilot, Prism, and Haul connected to Pulse with animated data flows">
            <svg class="hub-svg" viewBox="0 0 360 300" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <!-- Lines -->
              <line x1="106" y1="50" x2="140" y2="120" class="hub-line"/>
              <line x1="254" y1="50" x2="220" y2="120" class="hub-line"/>
              <line x1="180" y1="232" x2="180" y2="178" class="hub-line"/>

              <!-- Center: Pulse -->
              <rect x="120" y="120" width="120" height="58" rx="6" class="node-pulse"/>
              <text x="180" y="145" class="node-label-main">Pulse</text>
              <text x="180" y="163" class="node-label-sub">control plane · :9696</text>

              <!-- Pilot top-left -->
              <rect x="22" y="22" width="88" height="46" rx="6" class="node-app"/>
              <text x="66" y="42" class="node-label-app">Pilot</text>
              <text x="66" y="58" class="node-label-app-sub">TV · :8383</text>

              <!-- Prism top-right -->
              <rect x="250" y="22" width="88" height="46" rx="6" class="node-app"/>
              <text x="294" y="42" class="node-label-app">Prism</text>
              <text x="294" y="58" class="node-label-app-sub">Movies · :8282</text>

              <!-- Haul bottom -->
              <rect x="136" y="232" width="88" height="46" rx="6" class="node-app"/>
              <text x="180" y="252" class="node-label-app">Haul</text>
              <text x="180" y="268" class="node-label-app-sub">BitTorrent · :8484</text>

              <!-- Animated dots: heartbeat Pilot → Pulse -->
              <circle class="dot-hb" r="4">
                <animateMotion dur="2.8s" repeatCount="indefinite" begin="0s">
                  <mpath href="#p-pilot-pulse"/>
                </animateMotion>
              </circle>
              <!-- Config Pulse → Pilot -->
              <circle class="dot-cfg" r="4">
                <animateMotion dur="2.8s" repeatCount="indefinite" begin="1.4s">
                  <mpath href="#p-pulse-pilot"/>
                </animateMotion>
              </circle>

              <!-- Heartbeat Prism → Pulse -->
              <circle class="dot-hb" r="4">
                <animateMotion dur="3.2s" repeatCount="indefinite" begin="0.6s">
                  <mpath href="#p-prism-pulse"/>
                </animateMotion>
              </circle>
              <!-- Config Pulse → Prism -->
              <circle class="dot-cfg" r="4">
                <animateMotion dur="3.2s" repeatCount="indefinite" begin="1.9s">
                  <mpath href="#p-pulse-prism"/>
                </animateMotion>
              </circle>

              <!-- Stall events Haul → Pulse -->
              <circle class="dot-stall" r="3.5">
                <animateMotion dur="4.0s" repeatCount="indefinite" begin="1.2s">
                  <mpath href="#p-haul-pulse"/>
                </animateMotion>
              </circle>

              <defs>
                <path id="p-pilot-pulse" d="M 106,50 C 118,80 130,100 140,120" fill="none"/>
                <path id="p-pulse-pilot" d="M 140,120 C 130,100 118,80 106,50" fill="none"/>
                <path id="p-prism-pulse" d="M 254,50 C 242,80 230,100 220,120" fill="none"/>
                <path id="p-pulse-prism" d="M 220,120 C 230,100 242,80 254,50" fill="none"/>
                <path id="p-haul-pulse" d="M 180,232 L 180,178" fill="none"/>
              </defs>
            </svg>

            <div class="hub-legend">
              <span class="legend-item">
                <span class="legend-dot legend-dot--hb"></span>
                heartbeat / register
              </span>
              <span class="legend-item">
                <span class="legend-dot legend-dot--cfg"></span>
                config push
              </span>
              <span class="legend-item">
                <span class="legend-dot legend-dot--stall"></span>
                Haul registers (auto-wired download client)
              </span>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════
       REGISTRATION ANIMATION — capabilities up / config down
  ═══════════════════════════════════════════════ -->
  <section class="register-section">
    <div class="container">
      <div class="register-layout">

        <div class="register-text reveal">
          <p class="section-label">Registration</p>
          <h2 class="section-heading">Register once, configured automatically</h2>
          <p class="section-lead">
            When Pilot starts, it registers with Pulse: name, URL, capabilities
            (<code>content:tv</code>, <code>content:movies</code>, etc.). Pulse hands
            back the indexers, quality profiles, download clients, and shared
            media-handling settings that match those capabilities.
          </p>
          <p class="section-lead" style="margin-top:1rem;">
            Add an indexer to Pulse — its catalog category (Movies, TV) maps to the
            capability set, and Pulse auto-assigns it to every matching service. A
            new TV-only indexer reaches Pilot within 30 seconds; a Movies-and-TV
            one reaches both Pilot and Prism. One API key to update, not three.
          </p>
        </div>

        <div class="register-anim-wrap reveal">
          <div class="reg-anim" role="img" aria-label="Animation: Pilot sends capabilities to Pulse and receives a config bundle in response">

            <div class="reg-col">
              <div class="reg-col-label">
                <span class="reg-badge reg-badge--post">POST</span>
                Pilot registers
              </div>
              <div class="reg-blob" id="reg-blob-up">
                <div class="reg-blob-header">
                  <span class="reg-path">/api/v1/services/register</span>
                </div>
                <pre class="reg-code"><code><span class="syn-kw">&#123;</span>
  <span class="syn-string">"name"</span>:         "pilot",
  <span class="syn-string">"type"</span>:         "media-manager",
  <span class="syn-string">"api_url"</span>:      "http://pilot:8383",
  <span class="syn-string">"capabilities"</span>: [
    "content:tv",
    "supports_torrent",
    "supports_usenet"
  ]
<span class="syn-kw">&#125;</span></code></pre>
              </div>
            </div>

            <div class="reg-arrows" aria-hidden="true">
              <div class="reg-arrow reg-arrow--up">
                <svg viewBox="0 0 16 60" width="16" height="60">
                  <line x1="8" y1="55" x2="8" y2="12" stroke="#10b981" stroke-width="1.5" stroke-dasharray="3 3"/>
                  <polygon points="3,16 8,4 13,16" fill="#10b981"/>
                </svg>
              </div>
              <div class="reg-arrow reg-arrow--down">
                <svg viewBox="0 0 16 60" width="16" height="60">
                  <line x1="8" y1="5" x2="8" y2="48" stroke="#9898b0" stroke-width="1.5" stroke-dasharray="3 3"/>
                  <polygon points="3,44 8,57 13,44" fill="#9898b0"/>
                </svg>
              </div>
            </div>

            <div class="reg-col">
              <div class="reg-col-label">
                <span class="reg-badge reg-badge--ok">200</span>
                Pulse responds
              </div>
              <div class="reg-blob" id="reg-blob-down">
                <div class="reg-blob-header">
                  <span class="reg-path">config bundle</span>
                </div>
                <pre class="reg-code"><code><span class="syn-kw">&#123;</span>
  <span class="syn-string">"indexers"</span>: [
    &#123; "name": "NZBgeek",  "categories": ["TV"] &#125;,
    &#123; "name": "TorrentDB", "categories": ["TV"] &#125;
  ],
  <span class="syn-string">"quality_profiles"</span>: [
    &#123; "name": "HD-1080p", "managed_by_pulse": true &#125;
  ],
  <span class="syn-string">"download_clients"</span>: [
    &#123; "name": "haul", "host": "haul", "port": 8484 &#125;
  ],
  <span class="syn-string">"shared_settings"</span>: &#123;
    "rename_files":       true,
    "colon_replacement": "smart"
  &#125;
<span class="syn-kw">&#125;</span></code></pre>
              </div>
            </div>

          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════
       ECOSYSTEM ROW — one stack, four apps
  ═══════════════════════════════════════════════ -->
  <section class="ecosystem-section">
    <div class="container">
      <div class="section-header reveal">
        <p class="section-label">Architecture</p>
        <h2 class="section-heading">One stack, four apps</h2>
      </div>
      <div class="ecosystem-row">
        <div class="eco-step reveal">
          <div class="eco-icon-wrap">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="2" y="7" width="20" height="15" rx="2" ry="2"/>
              <polyline points="17 2 12 7 7 2"/>
            </svg>
          </div>
          <span class="eco-name">Pilot</span>
          <span class="eco-caption">TV series</span>
        </div>
        <div class="eco-line reveal" aria-hidden="true"></div>
        <div class="eco-step eco-step--hub reveal">
          <div class="eco-icon-wrap eco-icon-wrap--hub">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
            </svg>
          </div>
          <span class="eco-name">Pulse</span>
          <span class="eco-caption">Control plane</span>
        </div>
        <div class="eco-line reveal" aria-hidden="true"></div>
        <div class="eco-step reveal">
          <div class="eco-icon-wrap">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <polygon points="12 2 2 7 12 12 22 7 12 2"/>
              <polyline points="2 17 12 22 22 17"/>
              <polyline points="2 12 12 17 22 12"/>
            </svg>
          </div>
          <span class="eco-name">Prism</span>
          <span class="eco-caption">Movies</span>
        </div>
        <div class="eco-line reveal" aria-hidden="true"></div>
        <div class="eco-step reveal">
          <div class="eco-icon-wrap">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
              <polyline points="7 10 12 15 17 10"/>
              <line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
          </div>
          <span class="eco-name">Haul</span>
          <span class="eco-caption">BitTorrent</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════
       WHY IT EXISTS — 3 feature cards
  ═══════════════════════════════════════════════ -->
  <section class="why-section">
    <div class="container">
      <div class="section-header reveal">
        <p class="section-label">Why it exists</p>
        <h2 class="section-heading">Built for people who run their own stack</h2>
      </div>

      <div class="feature-grid">

        <div class="feature-card reveal">
          <div class="icon-chip">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/>
            </svg>
          </div>
          <h3>Register once, configure everywhere</h3>
          <p>
            Add an indexer to Pulse and every service whose capabilities match picks it
            up — within 30 seconds, or instantly via push hook. One API key, one
            quality profile, one place to change things when they break.
          </p>
        </div>

        <div class="feature-card reveal">
          <div class="icon-chip">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <circle cx="12" cy="12" r="10"/>
              <line x1="2" y1="12" x2="22" y2="12"/>
              <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
            </svg>
          </div>
          <h3>Standalone or connected</h3>
          <p>
            Pilot, Prism, and Haul each run fine with local config. Pulse is purely additive
            — bring it in when you're ready for the shared layer.
          </p>
        </div>

        <div class="feature-card reveal">
          <div class="icon-chip">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#10b981" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
              <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
            </svg>
          </div>
          <h3>Zero telemetry, MIT licensed</h3>
          <p>
            No analytics, no crash reporting, no update checks. Everything runs in your
            network and stays there. All four apps are MIT licensed and built in the open.
          </p>
        </div>

      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════
       GETTING STARTED
  ═══════════════════════════════════════════════ -->
  <section class="start-section">
    <div class="container">
      <div class="start-inner">

        <div class="start-text reveal">
          <p class="section-label">Getting started</p>
          <h2 class="section-heading">Up in three commands</h2>
          <p class="section-lead">
            The full stack — Postgres, Pulse, Pilot, Prism, Haul, and an optional
            VPN container — lives in <code>beacon-stack/deploy</code>. Point three
            paths at your media directories and run.
          </p>
          <a
            href="https://github.com/beacon-stack/deploy"
            class="btn btn--primary"
            style="margin-top:1.5rem;"
            target="_blank"
            rel="noopener noreferrer"
          >
            beacon-stack/deploy &rarr;
          </a>
        </div>

        <div class="start-terminal-wrap reveal">
          <div class="terminal">
            <div class="terminal-bar">
              <span class="terminal-dot terminal-dot--red"></span>
              <span class="terminal-dot terminal-dot--yellow"></span>
              <span class="terminal-dot terminal-dot--green"></span>
              <span class="terminal-title">terminal</span>
              <button class="terminal-copy-btn" type="button" aria-label="Copy commands">Copy</button>
            </div>
            <pre class="terminal-body"><span class="syn-comment"># Clone and configure</span>
<span class="syn-prompt">$ </span><span class="syn-cmd">git clone</span> https://github.com/beacon-stack/deploy
<span class="syn-prompt">$ </span><span class="syn-cmd">cd</span> deploy &amp;&amp; <span class="syn-cmd">cp</span> .env.example .env
<span class="syn-comment"># Set TV_PATH, MOVIES_PATH, DOWNLOADS_PATH in .env</span>

<span class="syn-comment"># Start everything</span>
<span class="syn-prompt">$ </span><span class="syn-cmd">docker compose up</span> <span class="syn-flag">-d</span></pre>
          </div>
        </div>

      </div>
    </div>
  </section>

</Base>

<style>
  /* ── Hero ────────────────────────────────────────────── */
  .hero {
    position: relative;
    padding: 120px 0 100px;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    top: -80px;
    left: 50%;
    transform: translateX(-50%);
    width: 700px;
    height: 500px;
    background: radial-gradient(ellipse at center, rgba(16,185,129,0.12) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-center {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.3em 0.875em;
    border-radius: 999px;
    font-size: 0.8125rem;
    font-weight: 500;
    background-color: var(--color-surface-2);
    border: 1px solid var(--color-border-2);
    color: var(--color-fg-2);
    margin-bottom: 1.75rem;
  }

  .hero-badge .live-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background-color: var(--color-green);
    box-shadow: 0 0 6px var(--color-green);
    animation: live-pulse 2.4s ease-in-out infinite;
    flex-shrink: 0;
  }

  @keyframes live-pulse {
    0%, 100% { opacity: 1; }
    50%       { opacity: 0.4; }
  }

  .hero-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 900;
    letter-spacing: -0.04em;
    line-height: 1.05;
    margin-bottom: 1.5rem;
  }

  .hero-sub {
    font-size: 1.125rem;
    color: var(--color-fg-2);
    line-height: 1.75;
    max-width: 56ch;
    margin-bottom: 2rem;
  }

  .hero-sub code {
    color: var(--color-accent);
    background: var(--color-accent-dim);
    border: 1px solid var(--color-accent-glow);
    font-size: 0.9em;
    padding: 0.1em 0.35em;
    border-radius: 3px;
    font-family: var(--font-mono);
  }

  .hero-actions {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    justify-content: center;
    margin-bottom: 2rem;
  }

  .hero-terminal {
    width: 100%;
    max-width: 480px;
  }

  /* ── Stats strip ────────────────────────────────────── */
  .stats-section {
    padding: 0 0 5rem;
  }

  .stats-strip {
    display: flex;
    align-items: stretch;
    border: 1px solid var(--color-border-2);
    border-radius: 10px;
    overflow: hidden;
    background-color: var(--color-surface);
  }

  .stat-item {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 2rem 1rem;
    gap: 0.375rem;
  }

  .stat-value {
    font-size: 2.5rem;
    font-weight: 900;
    letter-spacing: -0.04em;
    color: var(--color-accent);
    line-height: 1;
  }

  .stat-label {
    font-size: 0.875rem;
    color: var(--color-fg-2);
    font-weight: 500;
  }

  .stat-divider {
    width: 1px;
    background-color: var(--color-border-2);
    align-self: stretch;
  }

  /* ── Stack section ──────────────────────────────────── */
  .stack-section {
    padding: 100px 0;
    border-top: 1px solid var(--color-border);
  }

  .section-header {
    margin-bottom: 3rem;
  }

  .app-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }

  .app-card {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    background-color: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 10px;
    padding: 1.5rem;
    text-decoration: none;
    transition: border-color 0.2s ease, background-color 0.2s ease;
    cursor: pointer;
  }

  .app-card:hover {
    border-color: var(--color-border-2);
    background-color: var(--color-surface-2);
  }

  .app-card--pulse {
    border-color: var(--color-accent-glow);
    background-color: rgba(16,185,129,0.04);
  }

  .app-card--pulse:hover {
    border-color: var(--color-accent);
    background-color: rgba(16,185,129,0.07);
  }

  .app-card-header {
    display: flex;
    align-items: center;
    gap: 0.875rem;
  }

  .app-card .icon-chip {
    width: 40px;
    height: 40px;
    border-radius: 8px;
    background-color: var(--color-accent-dim);
    border: 1px solid var(--color-accent-glow);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: var(--color-accent);
  }

  .app-name {
    display: block;
    font-size: 1.0625rem;
    font-weight: 700;
    color: var(--color-fg);
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  .app-tag {
    display: inline-block;
    font-size: 0.6875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-accent);
    background-color: var(--color-accent-dim);
    border: 1px solid var(--color-accent-glow);
    padding: 0.15em 0.5em;
    border-radius: 4px;
    margin-top: 0.2rem;
  }

  .app-role {
    display: block;
    font-size: 0.8125rem;
    color: var(--color-fg-3);
    margin-top: 0.1rem;
  }

  .app-desc {
    font-size: 0.875rem;
    color: var(--color-fg-2);
    line-height: 1.65;
    flex: 1;
  }

  .app-port {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-fg-3);
  }

  .app-link {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-accent);
    transition: opacity 0.12s;
  }

  .app-card:hover .app-link {
    opacity: 0.8;
  }

  /* ── Flow section ───────────────────────────────────── */
  .flow-section {
    padding: 100px 0;
    border-top: 1px solid var(--color-border);
  }

  .flow-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .anim-panel {
    background-color: var(--color-surface);
    border: 1px solid var(--color-border-2);
    border-radius: 12px;
    padding: 2rem;
  }

  .hub-svg {
    width: 100%;
    height: auto;
    display: block;
  }

  /* SVG node styles */
  .hub-line {
    stroke: var(--color-border-2);
    stroke-width: 1.5;
    stroke-dasharray: 4 4;
  }

  .node-pulse {
    fill: rgba(16,185,129,0.08);
    stroke: #10b981;
    stroke-width: 1.5;
  }

  .node-app {
    fill: var(--color-surface-2);
    stroke: var(--color-border-2);
    stroke-width: 1.5;
  }

  .node-label-main {
    text-anchor: middle;
    dominant-baseline: middle;
    font-family: Inter, system-ui, sans-serif;
    font-size: 13px;
    font-weight: 700;
    fill: #10b981;
    letter-spacing: -0.02em;
  }

  .node-label-sub {
    text-anchor: middle;
    dominant-baseline: middle;
    font-family: "JetBrains Mono", monospace;
    font-size: 9px;
    fill: #5a5a72;
  }

  .node-label-app {
    text-anchor: middle;
    dominant-baseline: middle;
    font-family: Inter, system-ui, sans-serif;
    font-size: 12px;
    font-weight: 600;
    fill: #f0f0f5;
    letter-spacing: -0.01em;
  }

  .node-label-app-sub {
    text-anchor: middle;
    dominant-baseline: middle;
    font-family: "JetBrains Mono", monospace;
    font-size: 8.5px;
    fill: #5a5a72;
  }

  .dot-hb {
    fill: #10b981;
    filter: drop-shadow(0 0 4px #10b981);
  }

  .dot-cfg {
    fill: #9898b0;
    filter: drop-shadow(0 0 3px #9898b0);
  }

  .dot-stall {
    fill: #fbbf24;
    filter: drop-shadow(0 0 3px #fbbf24);
  }

  .hub-legend {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-top: 1.25rem;
    flex-wrap: wrap;
  }

  .legend-item {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.75rem;
    color: var(--color-fg-3);
  }

  .legend-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .legend-dot--hb    { background-color: #10b981; box-shadow: 0 0 5px #10b981; }
  .legend-dot--cfg   { background-color: #9898b0; }
  .legend-dot--stall { background-color: #fbbf24; }

  /* ── Register section ───────────────────────────────── */
  .register-section {
    padding: 100px 0;
    border-top: 1px solid var(--color-border);
  }

  .register-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .section-lead code {
    color: var(--color-accent);
    background: var(--color-accent-dim);
    border: 1px solid var(--color-accent-glow);
    font-size: 0.875em;
    padding: 0.1em 0.35em;
    border-radius: 3px;
    font-family: var(--font-mono);
  }

  .reg-anim {
    display: flex;
    gap: 0.75rem;
    align-items: flex-start;
    background-color: var(--color-surface);
    border: 1px solid var(--color-border-2);
    border-radius: 12px;
    padding: 1.5rem;
  }

  .reg-col {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .reg-col-label {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-fg-2);
  }

  .reg-badge {
    font-size: 0.6875rem;
    font-weight: 700;
    padding: 0.15em 0.45em;
    border-radius: 3px;
    letter-spacing: 0.02em;
  }

  .reg-badge--post { background: rgba(59,158,255,0.15); color: #3b9eff; }
  .reg-badge--ok   { background: rgba(52,211,153,0.15); color: #34d399; }

  .reg-blob {
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    overflow: hidden;
    flex: 1;
  }

  .reg-blob-header {
    padding: 0.4rem 0.75rem;
    background-color: var(--color-surface-2);
    border-bottom: 1px solid var(--color-border);
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .reg-path {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-fg-3);
  }

  .reg-code {
    margin: 0;
    padding: 0.75rem;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    line-height: 1.65;
    color: var(--color-fg-2);
    overflow-x: auto;
    white-space: pre;
  }

  .reg-arrows {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    padding-top: 2rem;
    flex-shrink: 0;
  }

  .reg-arrow {
    animation: arrow-pulse 1.8s ease-in-out infinite;
  }

  .reg-arrow--down {
    animation-delay: 0.9s;
  }

  @keyframes arrow-pulse {
    0%, 100% { opacity: 1; }
    50%       { opacity: 0.4; }
  }

  /* ── Ecosystem row ──────────────────────────────────── */
  .ecosystem-section {
    padding: 100px 0;
    border-top: 1px solid var(--color-border);
  }

  .ecosystem-row {
    display: flex;
    align-items: center;
    gap: 0;
    margin-top: 2.5rem;
    overflow-x: auto;
    padding-bottom: 0.5rem;
  }

  .eco-step {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.625rem;
    flex-shrink: 0;
  }

  .eco-icon-wrap {
    width: 56px;
    height: 56px;
    border-radius: 12px;
    background-color: var(--color-surface-2);
    border: 1px solid var(--color-border-2);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-fg-2);
  }

  .eco-icon-wrap--hub {
    background-color: var(--color-accent-dim);
    border-color: var(--color-accent);
    color: var(--color-accent);
    box-shadow: 0 0 20px rgba(16,185,129,0.15);
  }

  .eco-name {
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--color-fg);
    letter-spacing: -0.01em;
  }

  .eco-step--hub .eco-name {
    color: var(--color-accent);
  }

  .eco-caption {
    font-size: 0.75rem;
    color: var(--color-fg-3);
  }

  .eco-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(to right, var(--color-border-2), var(--color-border-2));
    min-width: 40px;
    position: relative;
    top: -28px; /* align with icon centers */
  }

  /* ── Why section ────────────────────────────────────── */
  .why-section {
    padding: 100px 0;
    border-top: 1px solid var(--color-border);
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin-top: 3rem;
  }

  /* ── Start section ──────────────────────────────────── */
  .start-section {
    padding: 100px 0;
    border-top: 1px solid var(--color-border);
  }

  .start-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .start-terminal-wrap {
    width: 100%;
  }

  /* ── Responsive ─────────────────────────────────────── */
  @media (max-width: 900px) {
    .hero {
      padding: 80px 0 64px;
    }

    .flow-layout,
    .register-layout,
    .start-inner {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .app-grid {
      grid-template-columns: 1fr;
    }

    .feature-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .stack-section,
    .flow-section,
    .register-section,
    .ecosystem-section,
    .why-section,
    .start-section {
      padding: 64px 0;
    }

    .eco-line {
      min-width: 20px;
    }
  }

  @media (max-width: 600px) {
    .hero-title {
      font-size: 2.25rem;
    }

    .feature-grid {
      grid-template-columns: 1fr;
    }

    .stats-strip {
      flex-direction: column;
    }

    .stat-divider {
      width: auto;
      height: 1px;
    }

    .ecosystem-row {
      flex-direction: column;
      gap: 1.5rem;
    }

    .eco-line {
      display: none;
    }
  }
</style>
